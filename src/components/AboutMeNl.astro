---
/**
 * Component met achtergrondinformatie en diensten in het Nederlands.
 * Portretbeeld via de `imageSrc`-prop.
 */
const {
imageSrc = "/images/Omnie.jpg",
  years = "10+",
} = Astro.props;
---
<section class="experience">
  <div class="copy">
    <span class="star">*</span>
    <h2>
      Ik richt me op praktische trainingen, advies en sterke fundamenten
    </h2>

    <p>
      Daarom bouwen we elk traject op echte zakelijke uitdagingen
      en tools die u direct binnen uw organisatie kunt inzetten.
    </p>

    <p>
      Ik werk met organisaties in heel Europa en lever trainingen op locatie en op afstand.
    </p>

    <ul class="services">
      <li>Praktische AI in begrijpelijke taal</li>
      <li>Ik pas me aan de behoeften van uw team aan</li>
      <li>Ik geef prioriteit aan toepasbare oplossingen</li>
    </ul>
  </div>

  <div class="image-wrapper">
    <img src={imageSrc} alt="Portret – Błażej Kunke" loading="lazy" decoding="async" width="420" height="560" />
  </div>
</section>

<style>
:root {
  --accent: #004b40;
  --radius-xl: 1rem;
}

.experience {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 2rem;
  padding: 1.5rem 1.5rem 2rem 1.5rem;
  max-width: 1200px;
  margin-inline: auto;
}

.copy .star {
  display: inline-block;
  color: var(--accent);
  font-size: 1.75rem;
  margin-bottom: 1rem;
}

.copy h2 {
  font-size: 1.65rem;
  line-height: 1.3;
  margin-bottom: 1rem;
}

.copy strong {
  font-weight: 700;
}

.copy p {
  margin-bottom: 1rem;
  color: #555;
}

.services {
  list-style: none;
  padding: 0;
  margin: 0;
}

.services li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.5rem;
}

.services li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: var(--accent);
}

.image-wrapper {
  border-radius: var(--radius-xl);
  overflow: hidden;
  max-width: 420px;
  justify-self: center;
}

.image-wrapper img {
  width: 100%;
  height: auto;
  display: block;
}

@media (max-width: 768px) {
  .experience {
    grid-template-columns: 1fr;
    text-align: center;
  }
  .image-wrapper {
    margin-top: 2rem;
  }
  .copy h2 {
    font-size: 1.45rem;
  }
}
</style>
