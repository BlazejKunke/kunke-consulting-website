---
/**
 * Reusable component that highlights professional background and services.
 * Pass your portrait image via the `imageSrc` prop.
 */
const {
imageSrc = "/images/Omnie.jpg", // update path as needed
  years = "10+",
} = Astro.props;
---
<section class="experience">
  <div class="copy">
    <span class="star">*</span>
    <h2>
      Stawiam na praktyczne szkolenia i doradztwo, i pracę u podstaw
    </h2>

    <p>
      Dlatego moje szkolenia i projekty są oparte na rzeczywistych problemach biznesowych, oraz na istniejących narzędziach, które można od razu wdrożyć w firmie.
    </p>

    <ul class="services">
      <li>Pracuję po polsku i po angielsku</li>
      <li>Dostosowuję się do potrzeb zespołu</li>
      <li>Stawiam na praktyczne rozwiązania</li>
    </ul>
  </div>

  <div class="image-wrapper">
    <img src={imageSrc} alt="Portrait" />
  </div>
</section>

<style>
:root {
  /* Adjust these to match your design system */
  --accent: #004b40;
  --radius-xl: 1rem;
}

.experience {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: center;
  gap: 2rem;
  padding: 1.5rem 1.5rem 2rem 1.5rem;
  max-width: 1200px;
  margin-inline: auto;
}

/* Text block */
.copy .star {
  display: inline-block;
  color: var(--accent);
  font-size: 1.75rem;
  margin-bottom: 1rem;
}

.copy h2 {
  font-size: 1.65rem;
  line-height: 1.3;
  margin-bottom: 1rem;
}

.copy strong {
  font-weight: 700;
}

.copy p {
  margin-bottom: 1rem;
  color: #555;
}

.services {
  list-style: none;
  padding: 0;
  margin: 0;
}

.services li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.5rem;
}

.services li::before {
  content: "✓";
  position: absolute;
  left: 0;
  color: var(--accent);
}

/* Image */
.image-wrapper {
  border-radius: var(--radius-xl);
  overflow: hidden;
  max-width: 420px;
  justify-self: center;
}

.image-wrapper img {
  width: 100%;
  height: auto;
  display: block;
}

/* Responsive */
@media (max-width: 768px) {
  .experience {
    grid-template-columns: 1fr;
    text-align: center;
  }
  .image-wrapper {
    margin-top: 2rem;
  }
  .copy h2 {
    font-size: 1.45rem;
  }
}
</style>
